apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: guacamole
  name: guacd-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 512Mi
