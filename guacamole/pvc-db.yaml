apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: guacamole
  name: guacamole-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 512Mi
